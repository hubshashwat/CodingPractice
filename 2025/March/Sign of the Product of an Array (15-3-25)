link: https://leetcode.com/problems/sign-of-the-product-of-an-array/description/

int arraySign(int* nums, int numsSize) {
 int i,sign;long double pro=1;
 for(i=0;i<numsSize;i++)
 pro*=nums[i];
 if(pro>0)
 sign=1;
 else if(pro<0)
 sign=-1;
 else
 sign=0;
 return sign;

}
